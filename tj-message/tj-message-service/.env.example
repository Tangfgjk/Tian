# 复制本文件为同目录下的 .env，并填入真实密钥。
# .env 已被 .gitignore 忽略，不会提交到仓库。
# 对应 tj-message-service/src/main/resources/bootstrap.yml 中的 ${TJ_SMS_ALI_*} 占位符。
TJ_SMS_ALI_ACCESS_ID=your-aliyun-access-id
TJ_SMS_ALI_ACCESS_SECRET=your-aliyun-access-secret
